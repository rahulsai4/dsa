#include<bits/stdc++.h>
using namespace std;

int main(){
    int t, n;
    cin >> t;
    while(t-- > 0){
        cin >> n;
        vector<int> dp(n + 1, -1);
        dp[0] = 1;
        dp[1] = 2;
    }
}
